# Copia este archivo como .env y sustituye el valor de ejemplo.
# La clave se utiliza exclusivamente en el servidor para generar recetas.
GEMINI_API_KEY="tu_clave_de_gemini"

# Puerto opcional del servidor Express (valor predeterminado: 3000).
PORT=3000
